import React from "react";
import { Card } from "antd";

// Componente de página de histórico de computadores
const Exceptions: React.FC = () => {
  return (
    <Card
      style={{
        width: "100%",
        height: "100%", 
        borderRadius: "10px",
        border: "1px solid #333", 
        margin: "10px",
      }}
    >
        Exceções
    </Card>
  );
};

export default Exceptions; // Exporta o componente de histórico de computadores